# Backend - API Server
web: uvicorn web_api:app --host 0.0.0.0 --port $PORT

# Celery Worker (if you are using celery)
# worker: celery -A worker.celery_app worker --loglevel=info

# SaaS Service (if this is separate)
# saas: python3 scripts/gnosis_service.py
