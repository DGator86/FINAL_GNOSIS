# Core Framework (Required)
pydantic>=2.0.0,<3.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
PyYAML>=6.0
loguru>=0.7

# Data Processing (Core only - avoid heavy packages)
pandas>=1.5.0
numpy>=1.24.0

# Trading & Market Data (Optional - only if you need real data)
# alpaca-trade-api>=3.0.0
# alpaca-py>=0.9.0

# Web Framework (Required)
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
httpx>=0.24.0

# HTTP Clients
requests>=2.31.0

# Utilities
anyio>=4.0.0
typing_extensions>=4.0.0

# Optional - only uncomment if needed
# scikit-learn>=1.3.0
# scipy>=1.11.0
# polars>=0.20
