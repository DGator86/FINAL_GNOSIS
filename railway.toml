[build]
builder = "DOCKERFILE"

[deploy]
# Debug: list packages before starting to confirm installation
startCommand = "pip list && uvicorn web_api:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
