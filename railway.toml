[build]
builder = "DOCKERFILE"

[deploy]
# Use the debug runner to print env info before starting
startCommand = "python debug_runner.py"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
